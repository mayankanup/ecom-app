spring:
  application:
    name: payment-service

server:
  port: 8081

app:
  # A local stand-in for a real Stripe-style secret key. In real life this would identify
  # the merchant account; here it just has to match what backend sends.
  api-key: ${PAYMENT_API_KEY:sk_test_51_local_dev_key}
